venv/
.venv/
__pycache__/
*.pyc

# Sessions (server-side Flask-Session)
flask_session/
flask_session*/
flask_session_data/

# Local database (contains data and passwords) — deploy separately on each server
taskun.sqlite
taskun.sqlite-*
*.sqlite-journal

# Docker image export (build locally: docker save po-taskun:latest -o po-taskun-latest.tar)
po-taskun-latest.tar
*.tar

# Secrets
.env
.env.*
!.env.example

# Archived Django + React — local artifacts only
_archive/**/db.sqlite3
_archive/**/__pycache__/
_archive/**/node_modules/
_archive/**/dist/
_archive/**/.env
_archive/**/.env.*

# IDE / OS
.vscode/
.idea/
Thumbs.db
Desktop.ini
